<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      xmlns:i18n="http://xml.zope.org/namespaces/i18n"
      lang="en"
      metal:use-macro="here/main_template/macros/master"
      i18n:domain="plone.app.content">
      <head>
        <div metal:fill-slot="javascript_head_slot" tal:omit-tag="">
          <script type="text/javascript">
              (function($){
                // Custom form constraints for constrain form
                $(document).ready(function(){
                  var allowed = $(".allow_form"),
                      allowed_2nd_step = $(".allow_2nd_step_form"),
                      constrain_mode = $(".constrain_types_mode_form"),
                      allowed_field = allowed.parents('.field'),
                      allowed_2nd_step_field = allowed_2nd_step.parents('.field'),
                      ACQUIRE = -1,
                      DISABLED = 0,
                      ENABLED = 1;
                  function updateVisibility(){
                    var mode = constrain_mode.val();
                    if(mode == ENABLED){
                      allowed_field.show();
                      allowed_2nd_step_field.show();
                    }else{
                      allowed_field.hide();
                      allowed_2nd_step_field.hide();
                    }
                  }
                  function updateSelectable(){
                    allowed.each(function(){
                      var allowed_2nd_step_id = this.id.replace('allow', 'allow_2nd_step'),
                          allowed_2nd_step_item = allowed_2nd_step_field.find("#" + allowed_2nd_step_id);
                      if (this.checked){
                        allowed_2nd_step_item[0].disabled = false;
                      }else{
                        allowed_2nd_step_item[0].disabled = true;
                        allowed_2nd_step_item[0].checked = false;
                      }
                    });
                  }
                  constrain_mode.change(updateVisibility);
                  updateVisibility();
                  allowed_field.change(updateSelectable);
                  updateSelectable();
                })
              }(jQuery));
          </script>
        </div>

      </head>
  <body>

    <metal:main fill-slot="main">
      <tal:main-macro metal:define-macro="main">

        <h1 class="documentFirstHeading" tal:content="view/label">Title</h1>


        <metal:block use-macro="context/@@ploneform-macros/titlelessform" />
      </tal:main-macro>
    </metal:main>
  </body>
</html>

